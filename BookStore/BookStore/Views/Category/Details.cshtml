@model BookStore.Contract.ResponseModels.CategoryResponseModel

@{
    ViewData["Title"] = "Category Detail";
    var index = 0;
}

<div class="text-center">
    <h1 class="display-4">Category Detail</h1>
</div>

<div>
    <dl class="row">
        <dt class="col-sm-2">
            Category Name
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CategoryName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
        <dt class="col-sm-2">
            Book(s)
        </dt>
        <dd class="col-sm-10">
            @if (!Model.Books.Any())
            {
                <p>There is no book that belongs to this category.</p>
            }
            @foreach (var item in Model.Books)
            {
                <a asp-controller="Home" asp-action="Details" asp-route-id="@item.BookId">@Html.DisplayFor(modelItem => item.BookName)</a>
                index++;
                if (index < Model.Books.Count())
                {
                    <br />
                }
            }
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Update" asp-route-id="@Model.CategoryId">Update</a> |
    <a asp-action="Index">Back to List</a>
</div>